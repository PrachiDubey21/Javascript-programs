//The founder of node js and deno js is Ryal dhal.



//------------------------------------------------------------------------------


//JavaScript is a dynamically typed language. 
This means that variable types are determined at runtime, 
and you do not need to explicitly declare the type of a variable 
before using it. You can assign different types of values to a variable 
during its lifetime.

For example, in JavaScript, you can do the following:
let x = 10; // x is now a number
x = "Hello"; // x is now a string
x = true; // x is now a boolean
On the other hand, statically typed languages 
require you to declare the variable's type explicitly, 
and the type checking is done at compile-time, before the code is executed.

Languages like Java, C++, and TypeScript are statically typed, 
and they require you to specify the variable type explicitly 
when declaring them



//----------------------------------------------------------------------



//execution flow

Javascript is single threaded (here everything is a process)
      -> global execution context
      -> Functional execution context
   -> eval execution context (property of global object)

   1. global execution phase
   2. Memory creation phase
   3. execution phase


//program flow

//global execution phase
   -> this

//Memory Creation phase(everything is initialized with null)

 for let var1 = 10;
    1. var1 = undefined
    2. same with Function
    3. same with function calls

//execution phase (initialized with data)

    1. var1 = 10;

    2. for function [new variable environment + execution thread]
                     // box like structure
                     //new executional context  
                     //again this box will go through every phase
                     //for the entire function that is being called

                     //(val1 , val2){
                        total
                     }

                     // first phase
                     //so val1 = val2 = total = undefined

                     //now second phase
                     //assign data

                     //returns the value if any in global execution context

                     //at last it gets deleted

    3. function calls gets there value calculated 




//call stack

//from bottom

1. global execution phase
2. methods that are called





//tailwind commands
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss@latest init -p
npm list tailwindcss
style.css -> @import "tailwindcss";


//js is a sequentially run language with an exception of "events"







//Object Oriented Programming
//js doesnot have classes
//it is a prototype-based language

//why use OOP

//parts of OOP 
// -> object literal
// -> constructor functions
// -> Prototypes
// -> classes
// -> instances (new , this)

//4 pillars
//Abstraction (fetch)
//inheritance ()
//Encapsulation (wrap up data)
//Polymorphism (many forms)







//Prototypal Behavior in JavaScript

Prototypal behavior refers to the way JavaScript objects inherit 
properties and methods from other objects through a mechanism called 
the prototype chain. Instead of using traditional class-based inheritance, 
JavaScript allows objects to inherit directly from other objects.

When a property or method is accessed on an object, JavaScript first
 looks for it in that object. If it is not found, JavaScript automatically 
 searches the object's prototype. This process continues up the prototype 
 chain until the property is found or the chain ends.

 const person = {
    greet() {
        console.log("Hello");
    }
};

const student = Object.create(person);

student.greet();

In this example, student does not have a greet() method of its own. 
JavaScript finds the method in person, which acts as its prototype,
 and executes it.






 //Prototype Inheritance in JavaScript

Prototype inheritance is the mechanism by which one object can 
inherit properties and methods from another object through the 
prototype chain. It allows objects to share functionality without 
duplicating code.

In JavaScript, every object has a hidden link to another 
object called its prototype. When a property or method is
not found in the current object, JavaScript automatically 
looks for it in the prototype. This process continues up the 
prototype chain until the property is found or the chain ends.

Example:

const person = {
    greet() {
        console.log("Hello");
    }
};

const student = Object.create(person);

student.name = "Prachi";

student.greet();

Here, student does not have its own greet() method. JavaScript 
looks in its prototype (person), finds greet(), and executes it. 
Thus, student inherits the method from person.


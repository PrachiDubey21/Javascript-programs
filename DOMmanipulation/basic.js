//the window object is the global object.
//It means everything in browser-side 
//JavaScript lives inside the window object.
//it is created by browser

//DOM (Document Object Model) is a tree-like structure
//created by the browser to represent your webpage.
//It allows JavaScript to access, change, 
//add, or delete any part of the webpage.
//DOM = bridge between HTML and JavaScript.
//we have tags that get converted into tokens
//by tokeniser then it gets converted into nodes like tree 
//multiple nodes makes DOM
// html -> js object -> document

//BOM (Browser Object Model) is everything in the 
//browser outside the webpage (outside the DOM).
//It gives JavaScript the power to
//interact with the browser itself, not just the HTML page.
//BOM (Browser Object Model) is a collection of objects provided 
//by the browser that allows JavaScript to
//interact with the browser window, control navigation, 
//manage history, access screen details, and use dialogs like 
//alert, prompt, and confirm

//DOM = controls the webpage
//BOM = controls the browser
